{
  "name": "renovationcostguide",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node server.js",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "dropbox": "^10.34.0",
    "express": "^4.21.2",
    "multer": "^2.1.1",
    "pdf-parse": "^2.4.5",
    "resend": "^6.9.3",
    "tesseract.js": "^7.0.0"
  }
}
